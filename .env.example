OPENAI_API_KEY=sk-replace-me
ANTHROPIC_API_KEY=sk-ant-replace-me
PORT=3005

# Set to "true" to run without any real API keys: embeddings and the action
# roadmap are generated locally via deterministic stubs (src/clients/mock.ts).
MOCK_MODE=false
